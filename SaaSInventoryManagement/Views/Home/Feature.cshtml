@using SaaSInventoryManagement.ViewModels.Navigation
@model NavItemDto
@{
    ViewData["Title"] = Model.DisplayName;
}

<div class="text-center py-5">
    <i class="@Model.IconClass fa-3x text-primary mb-3"></i>
    <h1>@Model.DisplayName</h1>
    <p class="text-muted">This feature is coming soon.</p>
    <p class="small text-muted">Permission key: <code>@Model.Key</code></p>
    <a asp-controller="Home" asp-action="Index" class="btn btn-outline-primary mt-2">Back to dashboard</a>
</div>