@using SaaSInventoryManagement.ViewModels.Warehouse
@model WarehouseFormVm
@{
    ViewData["Title"] = "New Warehouse";
}

<h1 class="h3 mb-4">New Warehouse</h1>

<div class="card shadow-sm" style="max-width: 560px;">
    <div class="card-body">
        <form asp-controller="Warehouse" asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="form-group mb-3">
                <label asp-for="Name"></label>
                <input asp-for="Name" class="form-control" autofocus />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="Code"></label>
                <input asp-for="Code" class="form-control" placeholder="e.g. WH-01" />
                <span asp-validation-for="Code" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="Address"></label>
                <textarea asp-for="Address" class="form-control" rows="3"></textarea>
                <span asp-validation-for="Address" class="text-danger"></span>
            </div>

            <div class="form-check mb-4">
                <input asp-for="IsActive" class="form-check-input" />
                <label asp-for="IsActive" class="form-check-label"></label>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">Create Warehouse</button>
                <a class="btn btn-outline-secondary" asp-controller="Warehouse" asp-action="Index">Cancel</a>
            </div>
        </form>
    </div>
</div>
